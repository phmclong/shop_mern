const Product = require("../models/product");

exports.getProducts = (req, res, next) => {
};

exports.postProducts = (req, res, next) => {
    const imageUrl = [];
    req.files.map(singleImageUrl => {
        imageUrl.push()
    })
    const title = req.body.title;
    const imageUrl = req.body.
    
};
